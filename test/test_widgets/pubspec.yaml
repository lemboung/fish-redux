name: test_widgets
description: widgets for redux test...

environment:
    sdk: '3.7.0-209.1.beta'

dependencies:
    flutter:
        sdk: flutter
    fish_redux:
        path: ../../

dev_dependencies:
    flutter_driver:
        sdk: flutter
    flutter_test:
        sdk: flutter
